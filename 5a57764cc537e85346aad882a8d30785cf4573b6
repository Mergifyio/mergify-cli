rebase onto main after #1499 merged: trivial textual conflict in junit_process/command.rs because main's  still used the inline  pattern that this commit refactors to ; took the refactor side and dropped a stale  line that's part of the now-merged Phase B work upstream
