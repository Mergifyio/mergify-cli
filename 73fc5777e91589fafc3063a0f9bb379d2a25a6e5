Switch the JUnit parser's text-event decoding from quick-xml's plain decode() to xml10_content() so CRLF line endings get normalized to LF per the XML 1.0 spec. Python's xml.etree.ElementTree did this normalization implicitly — without it the Windows-runner tests diff because git checks the fixture out with \r\n endings and the failure stacktraces ended up containing the \r bytes.
