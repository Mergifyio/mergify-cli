address Copilot review: (1) fix the latent bug ported from Python: duplicate-open-PR detection compared against state opened, never GitHubs actual open, so the InvalidState branch was dead; corrected the production code AND the test that confirmed the broken behavior (2) make --token optional and route through auth::resolve_token so the Python caller can pass MERGIFY_TOKEN via the subprocess env instead of argv, keeping the bearer token out of process listings
