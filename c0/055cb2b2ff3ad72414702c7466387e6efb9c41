code review: fixed the stale `tests show` exit-code table (claimed 1=flaky/6=broken; the command always exits 0, and 6 is this commit's own 403); moved the test under an admin banner instead of leaving it mislabelled as CI-scoped; dropped the doc claim that `tests/{test_id}` is covered (no live test reaches it); collapsed three repeated auth notes into one glossed Authentication section; added README + --help, which AGENTS.md makes the owners of the token contract.
