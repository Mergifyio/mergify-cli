address Copilot review: (1) defer uv tool dir subprocess until in-tree probes miss so the fast happy path doesn't pay a spawn; refactor through a testable locate_python_for_exe_with(query_uv) closure injection point and add laziness assertions; (2) rewrite PythonNotFound message from 'Set  to override' (contradictory when env var is already set but invalid) to 'Ensure  points to...' which is accurate for both unset and mis-set cases
