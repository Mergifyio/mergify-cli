Scrub GITHUB_OUTPUT in the canonical CI_ENV_VARS list — moved here from the Phase B expansion so PRs lower in the stack (#1465 parse JUnit XML) inherit the fix and stop hitting GHA's 'Matching delimiter not found' on scopes_detect tests.
