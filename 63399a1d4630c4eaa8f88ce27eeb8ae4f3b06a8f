Refactored Phase A from 'add Rust parser as library code (inert until Phase C)' into 'swap Python's XML parser for the native Rust parser via a hidden `_internal junit-parse` subprocess bridge'. Now this commit delivers concrete user-visible replacement instead of being build-only setup. Bridge plumbing will be deleted in Phase C when the Python side of junit-process goes away — cleanup checklist is in the commit body.
